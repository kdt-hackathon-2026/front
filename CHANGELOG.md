# CHANGELOG

## [작업 세션] 이모지→SVG, 신규 온보딩 단계, API 명세 반영, AI 도우미 오버레이

### 1. 이모지 → SVG 아이콘 전환
- `src/components/common/icons/registry.ts` 신설: 내비게이션·액션·상태·계좌/시나리오·은행 모티프 등 30종 라인 아이콘 정의
- `src/components/common/icons/Icon.vue` 신설: `name` prop으로 registry의 아이콘을 렌더링하는 공용 컴포넌트
- 아래 컴포넌트의 이모지(🐥 🔊 ✓ 🔍 ⌂ ‹ 🎤 ↺ ⏸ ▶ 🐢 💰 👛 🤝 🏥 🏠 ★ 🌾 📋 ⚙ ℹ️ ⚠️ 🔒 ✅ ✎ ⌫ 등)를 전부 `<Icon>` 컴포넌트로 교체
  - `AppButton`, `AppHeader`, `SelectCard`, `ConfirmRow`, `TipBox`, `NumericKeypad`, `BottomNav`, `BankQuickMenu`, `VoiceControlBar`, `VoiceInputModal`, `MascotTip`, `AiTutorView`
- `MascotCharacter.vue` 신설: 🐥 이모지를 대체하는 SVG 마스코트 캐릭터 (AI 도우미 오버레이·음성 모달·마스코트 팁에서 공용으로 사용)

### 2. "처음 설정" 단계 추가 (서비스 소개 + 이용약관 동의)
- `src/views/onboarding/IntroConsentView.vue` 신설: 서비스 소개 3항목 + 이용 전 확인사항 + 동의 체크박스
- 온보딩 순서 재편: **소개/동의(1/4) → 시작 방법(2/4) → 접근성 설정(3/4) → 은행 선택(4/4)**
- 라우터에 `/onboarding/intro` 경로 추가, 기존 3개 온보딩 화면의 StepProgress 번호를 1칸씩 밀어서 갱신

### 3. API 명세서 반영 (전체 타입/통신 계층 재작성)
- `src/types/index.ts` 전면 재작성: 홈/설정/AI튜터/이체연습 전 구간을 API 명세서의 요청·응답 스키마와 1:1로 맞춤
  - **새로 추가된 개념**: `AUTHENTICATION`(모의 인증) 단계, `Idempotency-Key` 기반 이체 실행, `PracticeReview`, `PracticeResult`, `SaveProgressRequest` 등
- API 계층을 3개 모듈로 분리
  - `api/homeApi.ts` — `GET /api/home`, `GET·PATCH /api/users/settings`, `GET /api/practices/summary`
  - `api/tutorApi.ts` — AI 튜터 8종 엔드포인트(guides/navigation/explain/speech/voice/events/hints/feedback)
  - `api/practiceApi.ts` — 이체연습 14종 엔드포인트 (시나리오/은행 조회 → 세션 시작 → 출금계좌 → 받는분 계좌 검증/확인 → 금액 검증 → 리뷰 → 인증 → 이체 실행 → 결과 → 진행저장/재개/재시작)
- `api/client.ts`: 응답 언랩 로직을 API 명세의 `{ success, message, data }` 포맷에 맞춰 수정, 에러 포맷도 `{ success:false, message, errorCode, data:null }`로 교체
- 모든 모듈은 `VITE_USE_MOCK` 스위치로 목데이터 폴백을 유지 (백엔드 연동 시 코드 수정 없이 전환)
- **중요한 트레이드오프**: API 명세서가 은행 코드/이름을 실제 값(`KB`, `KB국민은행` 등)으로 내려주는 것을 확인하고, 기존에 쓰던 가상 은행명("가은행/나은행") 대신 실제 값을 그대로 사용하도록 전환함. 이는 이전 세션에서 정했던 "실제 은행 상호 노출 금지" 방침을 뒤집는 결정이라 별도로 알려드림.

### 4. AI 도우미를 오버레이 방식으로 전면 개편
- `stores/tutor.ts`, `components/common/TutorOverlay.vue`, `router/screenMap.ts` 신설
- 기존에는 AI 튜터 버튼을 누르면 `/ai-tutor` 채팅 화면으로 이동했지만, 이제는 **현재 화면 위에 마스코트가 오버레이로 등장**해 순서대로(order 1, 2, 3…) 안내
  - 화면 진입 시 화면당 세션 1회 자동 등장, `data-tutor-id` 속성으로 지정된 실제 UI 요소를 찾아 강조(펄스 아웃라인) + 그 근처로 캐릭터가 이동
  - 오른쪽 아래 "AI 도우미" 버튼(`AiHelperFab`)을 누르면 현재 화면 가이드를 언제든 다시 볼 수 있음 (더 이상 다른 화면으로 이동하지 않음)
  - 이전/다음/건너뛰기 컨트롤, 음성 안내(TTS) 동시 재생
- 각 화면에 `data-tutor-id`를 부여: 홈의 이체 버튼, 온보딩 동의 체크박스, 은행 선택 리스트, 출금계좌 리스트, 받는분 은행/계좌 입력, 금액 키패드, 최종확인 체크리스트, 인증번호 입력, 완료화면 재시작 버튼, AI튜터 입력창 등

### 5. 은행 테마 적용 시점 변경 — "선택 즉시"가 아니라 "다음 화면부터"
- `useBankTheme.ts`는 그대로 두되, **호출부에서 테마를 언제 적용할지 직접 판단**하도록 정리
- 온보딩 은행 선택 화면(`BankSelectView`): 은행을 눌러도 화면 전체 테마는 바뀌지 않고, 별도의 "미리보기 카드" 안에서만 테마 색을 보여줌
- 이체연습 단계 화면(`TransferStepView`): 2단계(받는 분 은행 선택)에서 고른 은행 테마는 2단계 화면 자체에는 반영되지 않고, **3단계(금액 입력)로 넘어가는 순간부터** 헤더·버튼·진행바에 반영됨

### 6. 홈 화면 개편
- **"AI 튜터", "처음 배우기" 버튼 제거** — AI 튜터는 이제 모든 화면에 떠 있는 오버레이가 대신하고, "처음 배우기"는 온보딩 흐름(4단계)으로 대체
- `IntroMotion.vue` 신설: 첫 방문 시(또는 아직 못 본 경우) 홈 화면 상단에 마스코트가 통통 튀며 3줄 소개 문구가 순환하는 CSS/SVG 모션 그래픽을 보여줌 — "시작할게요"로 닫으면 다시 뜨지 않음(로컬 저장)
  - 실제 동영상 파일은 없어서 코드로 만드는 모션 그래픽으로 대체함 (요청하신 옵션 중 "코드로 만드는 모션 그래픽" 선택)
- `GET /api/home` 연동: 퀵메뉴, 이어할 연습(`resumePractice`) 등을 서버 응답 기준으로 표시

### 7. 이체연습 흐름 구조 변경 (API 명세와 정합성 확보)
- 기존에 "함께 해보기"/"스스로 해보기"가 각각 별도 파일(TogetherStepView/PracticeStepView 등)로 중복 구현되어 있던 것을, API가 완전히 동일한 이체연습 엔드포인트를 공유한다는 점에 맞춰 **공용 컴포넌트로 통합**
  - `views/practice/TransferIntroView.vue` — 함께/스스로 진입 화면 통합 (mode prop으로 문구만 분기)
  - `views/practice/TransferStepView.vue` — 5단계 통합 (출금계좌 → 받는분 계좌 → 금액 → 최종확인 → **모의 인증**)
  - `views/practice/TransferCompleteView.vue` — 완료 화면 통합
- 기존 `views/together/*`, 옛 `views/practice/PracticeStepView.vue` 등은 삭제
- 라우팅 경로(`/together/...`, `/practice/...`)는 그대로 유지해 기존 링크와 호환

### 8. 세션 스토어 재작성
- `stores/session.ts`를 API 명세의 `UserSettings`/`StepCode`/`practiceId` 모델에 맞춰 재작성 (`fontSize`→`textSize`, `voiceEnabled`→`voiceGuideEnabled`, `voiceSpeed`→`speechRate`, `helpLevel` 값 `first/guided/solo`→`BEGINNER/NORMAL/ADVANCED` 등)

---

### 남은 항목 / 알려드릴 점
- `IntroMotion`은 실제 영상이 아닌 CSS/SVG 애니메이션입니다. 실제 동영상 파일을 주시면 `<video>` 태그로 교체 가능합니다.
- AI 튜터 음성 안내(`/api/tutor/speech`)는 서버가 만든 오디오 URL을 재생하는 구조로 명세돼 있지만, 목데이터 모드에서는 브라우저 `speechSynthesis`(웹 스피치 API)로 그 자리를 대신하고 있습니다. 실제 백엔드 연동 시 오디오 URL 재생 로직을 추가해야 합니다.
- `/ai-tutor` 자유 질문 화면은 계속 존재하지만, 이제 AI 도우미 오버레이가 주된 안내 수단이라 홈에서 직접 연결하는 버튼은 없앴습니다. 필요하시면 다시 노출할 수 있습니다.

## [작업 세션 2] 은행 선택 선행, 화면 분리, 스스로 해보기 난이도 중심 개편

### 1. 화면 분리 — "은행 고르기"와 "계좌이체 연습"을 별도 페이지로
- 이전에는 이체연습 2단계 화면에서 "받는 분 은행 선택"과 "계좌번호 입력"을 한 화면에 같이 두었는데, 이를 분리했습니다.
- `views/practice/TransferBankSelectView.vue` 신설: 함께 해보기/스스로 해보기 모두 **가장 먼저** 이 화면에서 은행을 고릅니다. 여기서는 은행을 눌러도 화면 테마가 바뀌지 않습니다(미리보기 없이, 다음 화면부터 바로 반영).
- 이후 이체연습 5단계(`TransferStepView`)에서는 더 이상 "받는 분 은행"을 고르지 않고, 앞서 고른 은행 계좌번호만 입력합니다. (2단계 화면이 "은행+계좌" 통합에서 "계좌번호만" 입력하는 화면으로 단순화됨)

### 2. 은행 선택 직후 → 실제 뱅킹 앱처럼 UI 전환
- 예전에는 이체연습 3단계(금액 입력)부터 테마가 적용됐는데, 이제는 **은행을 고른 바로 다음 화면부터** 전 구간(단계 안내/난이도 선택 → 5단계 실습 → 완료 화면)이 그 은행 테마로 보입니다.
- KB국민은행 스타일(골드) 은행을 고르면 헤더·버튼·퀵메뉴가 곧바로 그 앱처럼 바뀌고, 출금 계좌 선택 화면에 실제 뱅킹 앱 홈 같은 퀵메뉴(조회/이체/상품가입/자산관리/전체)도 다시 보입니다.

### 3. "스스로 해보기" 개편 — 시나리오 선택 제거, 도움 난이도가 메인
- `views/practice/LevelSelectView.vue` 신설: 병원비/친목회비/생활비 같은 상황 선택 버튼을 없애고, **도움 없이 / 작은 도움(AI 일부 도움) / 도움(AI가 전부 도와줌)** 3개의 큰 카드형 버튼만 메인으로 배치
- 연습 문제(시나리오)는 고른 은행에 맞춰 자동으로 하나 골라 진행 (사용자가 따로 고르지 않음)
- 순서: **은행 선택 → 도움 난이도 선택 → 5단계 실습** (요청하신 순서대로 변경)

### 4. "처음 배우기"(함께 해보기) 명확화
- `TransferIntroView.vue`의 배지를 "처음 배우기"로, 안내 문구를 "AI가 화면마다 순서대로 다 알려드릴게요"로 정리해 이 흐름이 AI가 전부 안내하는 모드임을 명확히 했습니다.
- 함께 해보기는 도움 수준을 항상 `BEGINNER`(전체 안내)로 고정해서 시작합니다.

### 5. 완료 화면에서 소요 시간 제거
- `TransferCompleteView.vue`에서 "○분 ○초" 같은 소요 시간 표시를 제거했습니다 (도움 사용 횟수·오류 횟수만 남김).

### 라우팅 변경 요약
```
/together          은행 선택 (신규)
/together/start    처음 배우기 - 5단계 안내 + 시작
/together/step/1~5 이체연습 5단계
/together/complete 완료

/practice           은행 선택 (신규)
/practice/level     도움 난이도 선택 (신규, 시나리오 선택 대체)
/practice/step/1~5  이체연습 5단계
/practice/complete  완료
```

## [작업 세션 3] 오버레이 화면 밖 이탈 수정 + 오류 일괄 점검 + 불필요 파일 정리

### 1. "창이 앱 화면 밖으로 넘어가는" 문제 수정
- 원인: `#app`은 데스크톱에서 가운데 정렬된 480px 폭 컬럼인데, `TutorOverlay`(AI 도우미 캐릭터·말풍선)의 위치 계산이 `window.innerWidth/innerHeight`(브라우저 창 전체 기준)를 그대로 써서, 넓은 화면에서는 캐릭터/말풍선이 앱이 보이는 영역이 아니라 실제 브라우저 창 가장자리에 붙어버렸음
- `src/utils/appBounds.ts` 신설: `#app`의 실제 화면상 사각형(`getBoundingClientRect`)을 반환하는 헬퍼
- `TutorOverlay.vue`의 캐릭터/말풍선 위치·클램핑 로직을 전부 이 기준으로 교체, 창 크기 변경 시에도 다시 계산되도록 resize 리스너 추가
- `AiHelperFab`, `VoiceInputModal`은 점검 결과 이미 올바르게 앱 영역 안에 고정되고 있어 별도 수정 불필요

### 2. 실제 동작 오류 수정 (총 9건)
1. **AI 도우미 안내가 단계별로 안 바뀌던 문제** — 이체연습 1~5단계는 route.name이 그대로고 params.n만 바뀌는데, 오버레이는 route.name만 감시하고 있어서 어느 단계로 이동해도 처음 안내만 계속 보여줬음 → `route.fullPath`를 감시하고 단계 번호를 실제 StepCode로 변환하도록 수정
2. **`data-tutor-id` 위치 오류** — 서비스 소개 화면에서 안내가 실제 체크박스가 아니라 엉뚱한 카드에 표시되던 것을 수정
3. **더 이상 존재하지 않는 요소를 가리키던 안내 문구** — "받는 분 은행을 먼저 고르세요"라는 안내가 남아있었는데, 은행 선택이 별도 화면으로 분리되면서 그 요소 자체가 없어짐 → 안내 문구를 계좌번호 입력 절차에 맞게 다시 작성하고, 화면 코드도 `TRANSFER_PRACTICE`/`HELP_LEVEL`로 정확히 분리
4. **진행 단계가 서버에 항상 1단계로만 저장되던 문제** — `store.flow.step`이 연습 시작 시 한 번만 설정되고 이후 단계 이동 시 갱신되지 않아, `saveProgress` 호출이 항상 "출금 계좌 선택" 단계로만 보고되고 있었음 → 매 단계 이동마다 갱신하도록 수정
5. **홈 화면 '이어하기' 버튼이 실질적으로 동작하지 않던 문제** — 서버가 내려준 이전 진행 정보(은행/단계)를 쓰지 않고 화면에 남아있던 이전 로컬 상태만 참조하고 있어 사실상 항상 실패하는 구조였음 → `store.resumeFlow()` 신설, 서버가 준 practiceId/bankCode/currentStep으로 정확히 재개하도록 수정
6. **온보딩(소개·동의 → 접근성 → 은행) 전체가 홈 화면에서 도달 불가능했던 문제** — 첫 방문자가 메인 버튼을 눌러도 온보딩을 건너뛰고 바로 실습으로 들어가, 소개/동의/접근성 설정 화면들이 사실상 죽은 화면이었음 → 온보딩을 마치지 않은 사용자는 먼저 온보딩으로 안내하고, 온보딩 완료 시 자연스럽게 처음 배우기(함께 해보기)로 이어지도록 수정. "처음 배우기 다시 해보기" 링크도 홈에 추가해 온보딩 이후에도 계속 접근 가능하게 함
7. **'변경' 링크(기본 은행 재설정)가 의도치 않게 실습을 새로 시작시키던 문제** — 6번을 고치는 과정에서 생긴 부작용으로, 이미 온보딩을 마친 사용자가 기본 은행만 바꾸러 들어가도 완료 시 처음 배우기로 진입해버렸음 → 온보딩을 이미 마친 상태였는지 구분해서, 그런 경우엔 원래 있던 화면(홈)으로 돌아가도록 수정
8. **완료 화면의 "다른 상황 연습" 버튼이 더 이상 의미가 없던 문제** — 시나리오 선택 기능 자체를 없앴는데(작업 세션 2) 이 버튼만 남아있어 눌러도 실질적으로 아무것도 바뀌지 않았음 → 버튼 제거
9. **완료 화면의 "다른 은행 연습" 버튼이 온보딩 화면으로 잘못 연결되던 문제** — 스스로 해보기 도중에 눌러도 온보딩의 은행 설정 화면(그리고 8번과 얽혀 결과적으로 함께 해보기)으로 빠지고 있었음 → 스스로 해보기 전용 은행 선택 화면(`/practice`)으로 정확히 연결

### 3. 불필요한 파일 정리
- `components/common/VoiceControlBar.vue` 삭제 — 초기에 만들었지만 실제로는 어디에서도 쓰이지 않던 컴포넌트 (전체 소스에 대해 상호 참조 스크립트로 검증)
- `.vue`/`.ts` 파일 전체를 대상으로 미사용 파일 재검사 — 그 외 orphan 파일 없음 확인
- `TransferCompleteView.vue`의 이제 쓰이지 않는 CSS 클래스(`.footer-actions`) 정리

### 검증
- `vue-tsc -b --noEmit` 전체 통과 (오류 0건)
- `vite build` 전체 통과
- 클린 환경(새 폴더에 압축 해제 → `npm install` → `npm run build`)에서 재검증 완료
